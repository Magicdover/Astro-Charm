# 行尾规范：确保脚本在各平台都能正确运行
* text=auto

# Windows 批处理必须 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell 脚本必须 LF（CRLF 会导致 类Unix 上 "bad interpreter" 报错）
*.sh text eol=lf

# 二进制资源不做行尾转换
*.png binary
*.webp binary
*.ico binary
